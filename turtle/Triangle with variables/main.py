# Write a program in turtle that uses variables to draw a triangle.

import turtle

triangle = turtle.Turtle()

length = 100
angle = 120

triangle.forward(length)
triangle.left(angle)
triangle.forward(length)
triangle.left(angle)
triangle.forward(length)
