# Write a program in turtle that uses variables to draw a square.

import turtle

myTurtle = turtle.Turtle()

length = 100
angle = 90

myTurtle.forward(length)
myTurtle.left(angle)
myTurtle.forward(length)
myTurtle.left(angle)
myTurtle.forward(length)
myTurtle.left(angle)
myTurtle.forward(length)
